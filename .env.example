# =====================================================
# OpenMRS DLT Pipeline Environment Configuration
# =====================================================
# Copy this file to .env and customize for your environment
# Usage: cp .env.example .env

# =====================================================
# Airflow Configuration
# =====================================================

# Airflow admin credentials (default: admin/admin)
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin

# =====================================================
# MySQL/OpenMRS Database Configuration
# =====================================================

# MySQL root password
MYSQL_ROOT_PASSWORD=root

# OpenMRS database password (default: openmrs)
MYSQL_PASSWORD=openmrs

# =====================================================
# Notes
# =====================================================
#
# - The .env file is gitignored for security
# - These variables are used by docker-compose.yaml
# - For production, use strong passwords
# - Database credentials are also configured in dlt/.dlt/secrets.toml
