
[sources.sql_database.credentials]
drivername = "mysql+pymysql"  # database+dialect
database = "openmrs"
username = "openmrs"
password = "openmrs"
# For Docker Airflow deployment: use "openmrs-mysql" (service name)
# For standalone/local development: use "localhost" or "127.0.0.1"
host = "openmrs-mysql"
port = 3306  # Internal MySQL port (external is 3307)
